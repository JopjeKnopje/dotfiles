[user]
	name = joppe
	email = jopjeknopje@proton.me
	signingkey = 1E797962E571F07CEC19A0A7E5932D1B6DE0A3E2
[core]
	autocrlf = false
	pager = less -F -S -R -i -+X
[pager]
	diff = less +/diff\\ --git
[color]
	status = auto
	diff = auto
	branch = auto
[alias]
	st = status
	ci = commit
	co = checkout
	staged = diff --cached
	fpush = push --force-with-lease
  	open = "!f() { REPO_URL=$(git config remote.origin.url); xdg-open ${REPO_URL%%.git}; }; f"
  	browse = !git open
[branch]
	autosetupmerge = true
[init]
	defaultBranch = main
[push]
	recurseSubmodules = on-demand
	default = current
	autoSetupRemote = true
[submodule]
	recurse = true
[pull]
	rebase = true
[safe]
	directory = /etc
[commit]
	gpgsign = true
